name: path_graph
description: >
  Self-contained weighted-graph path determination for MeshCore DMs.
  Pure Dart, push-in-never-read-out — see docs/path-graph-design.md.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  drift: ^2.34.2

dev_dependencies:
  build_runner: ^2.4.13
  drift_dev: ^2.34.0
  lints: ^5.0.0
  test: ^1.25.8
